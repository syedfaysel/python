


name = "Faysel"
# name is a string

print(name)

# slicing
firstpart = name[0:3] # characters from position 0 (included) to 3 (excluded)
lastPart = name[3:] # characters from position 3 (included) to end

print(firstpart)
print(name[3:]) 


# Slice indices have useful defaults; 
# an omitted first index defaults to zero, 
# an omitted second index defaults to the size of the string being sliced.
